/**
 * Точка входа для всех парсеров кода
 * Позволяет в будущем добавлять новые парсеры (для других языков/фреймворков)
 */

const reactParser = require("./react");

module.exports = {
  // Парсер React компонентов
  ...reactParser,
};
