// Это пример файла, который тестируется

const a: number = 5;

let helloWorld: string | number = "Hello World";

interface User {
    name: string;
    id: number;
}

const user: User = {
    name: "Hayes",
    id: 0,
};